bootJar { enabled = false }
jar { enabled = true }

dependencies {
    api project(':gofield-common')
    api("org.springframework.cloud:spring-cloud-aws-messaging:${springCloudAwsVersion}")
    implementation("org.springframework.cloud:spring-cloud-aws-autoconfigure:${springCloudAwsVersion}")
}
