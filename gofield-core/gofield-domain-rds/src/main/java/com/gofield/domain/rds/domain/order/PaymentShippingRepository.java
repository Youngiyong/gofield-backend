package com.gofield.domain.rds.domain.order;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PaymentShippingRepository extends JpaRepository<PaymentShipping, Long> {
}
